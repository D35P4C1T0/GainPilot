diff --git a/distrho/src/DistrhoPluginAU.cpp b/distrho/src/DistrhoPluginAU.cpp
index 28a2d8de..5ec7ed31 100644
--- a/distrho/src/DistrhoPluginAU.cpp
+++ b/distrho/src/DistrhoPluginAU.cpp
@@ -532,0 +533,6 @@ public:
+            if (inScope == kAudioUnitScope_Global)
+            {
+                outDataSize = sizeof(Float64);
+                outWritable = true;
+                return noErr;
+            }
@@ -642 +648,3 @@ public:
-            DISTRHO_SAFE_ASSERT_UINT_RETURN(inScope == kAudioUnitScope_Input, inScope, kAudioUnitErr_InvalidScope);
+            // REAPER uses the legacy global-scope alias for effect input callbacks.
+            DISTRHO_SAFE_ASSERT_UINT_RETURN(inScope == kAudioUnitScope_Input || inScope == kAudioUnitScope_Global,
+                                            inScope, kAudioUnitErr_InvalidScope);
@@ -839,0 +848,5 @@ public:
+            if (inScope == kAudioUnitScope_Global)
+            {
+                *static_cast<Float64*>(outData) = fPlugin.getSampleRate();
+                return noErr;
+            }
@@ -1260 +1273,2 @@ public:
-            DISTRHO_SAFE_ASSERT_UINT_RETURN(inScope == kAudioUnitScope_Input || inScope == kAudioUnitScope_Output, inScope, kAudioUnitErr_InvalidScope);
+            DISTRHO_SAFE_ASSERT_UINT_RETURN(inScope == kAudioUnitScope_Global || inScope == kAudioUnitScope_Input || inScope == kAudioUnitScope_Output,
+                                            inScope, kAudioUnitErr_InvalidScope);
@@ -1262 +1276,2 @@ public:
-            DISTRHO_SAFE_ASSERT_UINT_RETURN(inScope == kAudioUnitScope_Output, inScope, kAudioUnitErr_InvalidScope);
+            DISTRHO_SAFE_ASSERT_UINT_RETURN(inScope == kAudioUnitScope_Global || inScope == kAudioUnitScope_Output,
+                                            inScope, kAudioUnitErr_InvalidScope);
@@ -1264 +1279,2 @@ public:
-            DISTRHO_SAFE_ASSERT_UINT_RETURN(inScope == kAudioUnitScope_Input, inScope, kAudioUnitErr_InvalidScope);
+            DISTRHO_SAFE_ASSERT_UINT_RETURN(inScope == kAudioUnitScope_Global || inScope == kAudioUnitScope_Input,
+                                            inScope, kAudioUnitErr_InvalidScope);
@@ -1272,0 +1289,14 @@ public:
+                if (inScope == kAudioUnitScope_Global)
+                {
+                   #if DISTRHO_PLUGIN_NUM_INPUTS != 0
+                    fSampleRateForInput = sampleRate;
+                   #endif
+                   #if DISTRHO_PLUGIN_NUM_OUTPUTS != 0
+                    fSampleRateForOutput = sampleRate;
+                   #endif
+                    d_nextSampleRate = sampleRate;
+                    fPlugin.setSampleRate(sampleRate, true);
+                    notifyPropertyListeners(inProp, inScope, inElement);
+                    return noErr;
+                }
+
@@ -1472 +1502,3 @@ public:
-            DISTRHO_SAFE_ASSERT_UINT_RETURN(inScope == kAudioUnitScope_Input, inScope, kAudioUnitErr_InvalidScope);
+            // REAPER uses the legacy global-scope alias for effect input callbacks.
+            DISTRHO_SAFE_ASSERT_UINT_RETURN(inScope == kAudioUnitScope_Input || inScope == kAudioUnitScope_Global,
+                                            inScope, kAudioUnitErr_InvalidScope);
@@ -1970,0 +2003,8 @@ public:
+           #if DISTRHO_PLUGIN_NUM_OUTPUTS != 0
+            // The input callback is allowed to replace AudioBuffer::mData with
+            // host-owned input storage. Preserve the normalized output pointers
+            // before it runs so processed audio reaches the supplied buffers.
+            for (uint16_t i = 0; i < numOutputs; ++i)
+                outputs[i] = static_cast<float*>(ioData->mBuffers[i].mData);
+           #endif
+
@@ -2012,6 +2051,0 @@ public:
-
-               #if DISTRHO_PLUGIN_NUM_OUTPUTS != 0
-                for (uint16_t i = 0; i < numOutputs; ++i)
-                    outputs[i] = static_cast<float*>(ioData->mBuffers[i].mData);
-               #endif
-
@@ -2023,5 +2056,0 @@ public:
-
-               #if DISTRHO_PLUGIN_NUM_OUTPUTS != 0
-                for (uint16_t i = 0; i < numOutputs; ++i)
-                    outputs[i] = static_cast<float*>(ioData->mBuffers[i].mData);
-               #endif
@@ -2028,0 +2058,5 @@ public:
+
+           #if DISTRHO_PLUGIN_NUM_OUTPUTS != 0
+            for (uint16_t i = 0; i < numOutputs; ++i)
+                ioData->mBuffers[i].mData = outputs[i];
+           #endif
@@ -2451 +2485,2 @@ private:
-            std::realloc(fAudioBufferList, sizeof(uint32_t) + sizeof(AudioBuffer) * numBuffers)))
+            std::realloc(fAudioBufferList,
+                         offsetof(AudioBufferList, mBuffers) + sizeof(AudioBuffer) * numBuffers)))
